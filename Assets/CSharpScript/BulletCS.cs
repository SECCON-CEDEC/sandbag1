using UnityEngine;
using System.Collections;

public class BulletCS : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Destroy(gameObject, 1); //1秒後にGameObjectを削除
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
